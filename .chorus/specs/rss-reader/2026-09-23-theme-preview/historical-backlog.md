# 历史待办推进记录（2026-09-24）

用户明确：包括历史总清单，先做本机可完成项。网络阶段已完成的本机证据见network-proxy-backoff.md；历史记录未勾选不等于产品未实现，逐项核对后回填。

## 本批证据

- spec三格式解析：新增命名空间content:encoded + CDATA + 两个enclosure同条目的样本，断言文章链接和稳定ID未被附件污染。现parse_formats共9条通过。
- spec身份稳定/去重：既有缺guid、缺guid及link的重复解析断言，配合store upsert_dedupes与fetch重复刷新无重复测试；回填完成。
- 发现真实编码回归：UTF-8字节错误声明GBK时旧结果为“涓…”，新增测试先失败；修复为完整合法UTF-8且声明GBK/GB18030时跳过冲突prolog，正常旧编码仍交给feed-rs解码，无新增依赖。样本含真正GB18030四字节字符𠀀、BOM/单引号错误声明及正常Latin-1。未知错误编码声明不作全面保证。
- spec订阅组织：代码已实现unsubscribeFeed→confirmDialog，原清单“删除二次确认未实现”已纠正；待补运行取消/确认断言。订阅拖拽排序确实缺失（只有标签排序）。

## 接下来可在本机推进

1. 订阅拖拽排序持久化与删除确认运行验证；不改现有标签排序。
2. 安全正文渲染：script/onerror/javascript:真实WebKit探针；离线阅读、键盘/剪贴板按会话证据区分。
3. 自动刷新墙钟/单flight/导入新增源和通知开关；500源与10k条目性能及冷启动单独测量，不能以小库替代。
4. 迁移/强杀恢复、原生备份恢复与便携数据目录；代理抓包白名单与剩余错误提示本地化。
5. 主题旧条目与T3至T7/后续原生测试证据逐项映射，复合项只覆盖一部分时保持未完成。

## 环境或用户参与依赖（保留）

- Windows/macOS发布安装、GNOME、纯Wayland无XWayland、缺libayatana环境；不把KDE/Xvfb结果代替。
- 原生输入法fcitx5/ibus、感知清晰度/滚动手感、读屏器与GUI客户端图片呈现，需要相应环境或人工观察。
- KWallet系统包升级需要系统授权；本轮不触发弹窗、不擅自升级。
- FreshRSS/Inoreader互导、MrRSS/Papr/Boke同口径性能需要对应软件/服务；不以自家回环测试替代。

本批完整Rust回归EXIT=0：433 passed / 0 failed（28段），日志归档摘要后清理。历史原始清单快照/tmp/rustrss-backlog-inventory.txt留待映射完成后清理。

## 订阅排序进度

v14新增feeds.position，NULL保留原名称序；core事务内执行同组相对移动，UI原生拖拽及上移/下移菜单共用move_feed。迁移/分组边界/新增订阅追加/重启持久化3条core测试与排序/删除确认2条Node测试通过；全量Rust436/0（29段）、Node51/0、clippy原有3警告。运行探针仍在排查无WM Xvfb拖拽坐标，未宣称真实拖拽通过。临时输出本轮根目录dk_8iuhb、3w_st76l、b1skidq4及probe3日志所列目录，收口时清理。

拖拽诊断更新：probe3使用绝对坐标分步移动后已收到目标行dragover，但仍没有drop，最终顺序未变；不能计通过。原生测试失败根目录新增nht7e31e。下一步检查dragover是否被接受、mouseup时目标与GTK DnD完成状态，必要时在原生会话隔离窗口验证；不因测试困难增加产品dragend猜测性落库。现所有本轮探针进程已退出。

## 排序与持久性最新结论

排序两档真实X11指针已通过；前述探针失败为历史诊断记录，最终证据见subscription-order-security.md。正文安全实际渲染与正对照通过；新增逐版本升级/强杀WAL恢复测试通过。下一项为自动刷新墙钟/单flight与性能库测量。

## 调度与性能最新结论

15分钟定时/关闭墙钟、启动刷新、单flight与正文锚点通过；500源/10k文章的5分钟PSS和刷新测量完成，SSD数据库冷页启动1.034秒。分组查询复现慢点后新增v15索引覆盖feed_id，冷页528ms→26ms，热查88ms→2.7ms；测试440/0、Node51/0。完整口径见scheduler-performance.md。

## 功能缺口与验收缺口分开跟踪

- 明确未实现：真实订阅条目缩略图元数据提取、保存与列表展示链路。
- 部分实现：后端错误码/双语覆盖仍未全面完成；凭据库系统包修复需要系统侧操作。
- 已实现待补验：键盘全流程、剪贴板双后端、离线阅读、10k搜索、原生备份/便携目录、外部OPML互导、部分AI/凭据库场景。
- 旧spec未勾不代表未实现：三栏/主题/条件刷新等需逐条映射既有证据，复合验收条件未齐全前保持开放。
- 下一项先补本机完整外呼记录和10k搜索/键盘离线验收，再推进缩略图与剩余错误本地化。Windows/macOS/GNOME等依赖条件保持开放。

## 键盘与搜索进度

核对时发现`?`帮助和侧栏行键盘焦点确实缺实现，现已补齐；真实X11键盘串联12项通过，附万篇稀疏英文FTS热查询2–4ms。完整键盘/搜索/断网验收仍开放，详见keyboard-search.md。Rust440/0、Node55/0。五个本轮隔离目录与构建/测试日志均已清理，未触及历史暂留目录；工作树改动仍未提交。

## 搜索反例与离线补验

生产upsert建立万篇中英文库，SSD零驻留页搜索发现：宽泛FTS冷查214–238ms、稀疏/无结果中文单字LIKE冷查661–706ms。即时搜索未达成，下一步处理单字全扫并分层宽泛排序开销。不可用早前稀疏英文2–4ms覆盖此反例。

真实独立网络命名空间仅lo（不改主机网络），15项按键/缓存阅读/刷新失败继续导航通过，10000条未丢；原生Wayland/外部图片等范围仍开放。见search-offline.md及两份结果JSON。

## 单字搜索优化

v16单字候选索引与旧库事务迁移已实现，保留标题/正文校验及纯单字时间顺序；冷页稀疏/无结果661–706ms降至16–18ms。常见字首次尝试曾退化249ms，现已修成34ms并有计划负对照。详见search-unigrams.md；下一步继续宽泛多字FTS分层与桌面单字输入验证。

## 宽泛搜索与桌面输入补验

v17将宽泛相关度候选的评分/排序/前200截取放在轻量覆盖索引阶段，之后才读取文章列表元数据；宽泛英文/中文SSD零驻留页首次65–77ms、热查约25ms，排序索引167,936字节。真实Xvfb输入到200行列表渲染95ms；loopback-only私有命名空间16项通过（含失败刷新后的缓存阅读），同一探针宽泛查询66ms。全workspace测试与Node55条通过，clippy仅原有3条。证据见search-unigrams.md、search-ranked-results.json、keyboard-search.md。

仍未闭合：原生Wayland搜索输入、release/Windows/macOS性能、全键盘主流程、外部图片离线；全文“即时”AC暂保持开放。待办优先转向本地完整网络外呼记录与尚未实现的订阅缩略图提取展示，按独立需求确认范围后处理；系统包、其它平台及需人工观察的体验继续保留。
