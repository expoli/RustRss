# 网络后续任务执行记录

## 基线

- 网络错误分类与双语提示已提交并推送：64d65fb。
- 用户要求继续完成后续待办，并明确加入应用内代理设置。
- 用户的 `.codex/` 文件不属于本轮，保持不动。

## 待完成与验收口径

1. 限流退避：解析429/503的Retry-After（秒数及HTTP日期）；等待状态持久化，重启不丢；后台定时与启动刷新尊重期限；验证过期、无效头、恢复和缓存保持。明确手动刷新是否绕过的产品语义，避免静默丢任务与错误成功计数。
2. 应用内代理：环境/直连/自定义；设置校验与持久化；桌面与MCP共享core配置，订阅抓取、发现、全文相关路径一致。明确AI请求范围与凭据存储，不能将代理密码意外写入错误日志。验证HTTP代理、HTTPS CONNECT、NO_PROXY、不可达与407，设置变更生效。
3. UI与文档：双语设置文案与可访问控件；变更后重建桌面并用隔离实例验证；README、spec与清单同步。
4. 回归：cargo test --workspace、Node、clippy；记录未覆盖平台，不把本地夹具当作真实外网环境证据。
5. 历史待办清点：旧清单包含后续已完成条目，需按最新证据去重；跨平台/人工环境依赖明确列出，不能空勾。

## 已获得证据

新增 `crates/rustrss-core/tests/proxy_environment.rs`：独立子进程隔离环境变量；真实生产Fetcher访问本地代理。HTTP_PROXY成功、NO_PROXY直连、代理407共3条测试通过（含子进程入口测试）。未修改产品代理实现，尚未验证HTTPS CONNECT。

## 限流实现进度

- v13新增feeds.retry_after_at；collect_jobs携带期限，fetch_jobs统一拒绝期限内网络请求，并报告retry_deferred，apply_results不覆盖上次真实抓取状态。手动请求不绕过期限。
- 已通过18条既有抓取测试、2条新增期限重启/恢复与v12升级测试。HTTP日期/秒数边界测试待收集结果；桌面重建验证与全量回归尚未完成。
- 旧扩展probe已增加期限内无请求断言，然后仅将隔离库期限置为过去，继续独立验证超时/TLS。

## 代理实现进度

- core/network.rs实现环境/直连/自定义HTTP(S)代理配置，拒绝含凭据地址，不持久化密码；已接桌面订阅/发现/全文/RSSHub探测、MCP抓取工具与AI客户端。
- Fetcher按配置复用连接池，配置变化只影响后续请求；MCP每次读取同一数据库配置。
- 订阅设置页已增加模式、代理地址、绕过列表和保存按钮，双语提示说明应用范围及不支持凭据地址。
- 定向测试4条通过：环境代理、NO_PROXY、407、自定义切换/直连；core配置回读/非法值不覆盖测试通过。Node既有47条通过。
- 尚未完成：HTTPS CONNECT/不可达、AI与MCP代理运行证据、设置页运行与持久化验证、新增控件测试、完整回归及文档收口。
- 当前全量Rust测试进程session 16068，日志/tmp/rustrss-network-next-tests.log；Node日志/tmp/rustrss-network-next-node.log，最终归档后只清本轮产物。

## 最新状态

网络实现与本机验收已收口，详见network-proxy-backoff.md；最终Rust430/0、Node49/0。以上“待收集/尚未完成”是执行时记录，以最新报告为准。用户已确认包含历史总清单，优先本机可完成项；不将跨平台项目擅自勾选。

## 历史清单下一批

先建立逐项证据映射，优先补本机可重复验证的解析/迁移/缓存/安全渲染/自动刷新及订阅删除确认和排序。原生窗口/托盘/输入法、系统升级、其它平台分别记录环境条件，不触发授权弹窗或擅自改系统。网络两档临时目录、截图、TLS私钥和本轮日志已清理，结构化证据已入仓库文档。原始未勾选项快照暂在/tmp/rustrss-backlog-inventory.txt，待整理后清理。
